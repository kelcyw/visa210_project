{
  "dependencies": {
    "stats.js": "^0.17.0",
    "three": "^0.158.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "vite": "^4.5.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "node -v && npm -v && npm clean-install && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d ./dist"
  }
}
